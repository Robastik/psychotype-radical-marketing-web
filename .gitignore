# CI/CD
.github/

Wait, that would ignore the workflow file that was modified. Since only a GitHub Actions workflow file was modified and this is a standard part of version control, nothing should be ignored.

(empty)